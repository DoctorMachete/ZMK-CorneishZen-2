#include "keycodes.h"

repeat_macro: repeat_macro {
    label = "repeat_macro";
    compatible = "zmk,behavior-macro";
    #binding-cells = <0>;
    bindings
        = <&macro_tap  &key_repeat>
        ;
};
macro_accent: macro_accent {                    //  ` → ^ (with shift)
    label = "macro_accent";
    compatible = "zmk,behavior-macro";
    #binding-cells = <0>;
    bindings
        = <&macro_tap &kp LEFT_BRACKET &kp SPACE>
        ;
};
macro_acute: macro_acute {                    //  ^ (with shift)
    label = "macro_acute";
    compatible = "zmk,behavior-macro";
    #binding-cells = <0>;
    bindings
        = <&macro_tap &kp KC_ACUTE &kp SPACE>
        ;
};

macro_ini_comment: macro_ini_comment {                    //  ^ (with shift)
    label = "macro_ini_comment";
    compatible = "zmk,behavior-macro";
    #binding-cells = <0>;
    bindings
        = <&macro_tap &kp KC_SLASH &kp KC_ASTRSK>
        ;
};
macro_end_comment: macro_end_comment {                    //  ^ (with shift)
    label = "macro_end_comment";
    compatible = "zmk,behavior-macro";
    #binding-cells = <0>;
    bindings
        = <&macro_tap &kp KC_ASTRSK &kp KC_SLASH>
        ;
};